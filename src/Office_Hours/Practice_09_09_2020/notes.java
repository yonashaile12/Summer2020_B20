package Office_Hours.Practice_09_09_2020;

public class notes {

    public static void main(String[] args) {
        /*
        Samsung IS A Phone
        Iphone IS A Phone
        Nokia IS A Phone


        access modifiers that are visible to the sub class
        public: is always visible
        protected: always visible(outside package, just for subclass)
        default: only visible within the same package
        private: it is never visible

        Phone: Brand, model, price, madeIn, call(), text()
        static: brand, madeIn
        instance: model, price, call(), text(), toString()

        overriding: one method with a different implementations
         */
    }
}
